name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.11.33"
          enable-cache: true
      - run: uv python install ${{ matrix.python-version }}
      - run: uv sync --frozen --python ${{ matrix.python-version }}
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy
      - run: uv run pytest -q
      - run: uv run python -m statement_lens.cli fixtures/synthetic_statement.json --output report.json
      - run: uv run python -m statement_lens.cli fixtures/synthetic_statement.json --output report-replay.json
      - run: cmp report.json report-replay.json
      - run: >-
          uv run python -m statement_lens.cli fixtures/synthetic_restatement.json
          --base-accession 0000000000-25-000001
          --comparison-accession 0000000000-25-000002
          --as-of 2025-03-02T00:00:00Z
          --output restatement.json
      - run: >-
          uv run python -m statement_lens.cli fixtures/synthetic_restatement.json
          --base-accession 0000000000-25-000001
          --comparison-accession 0000000000-25-000002
          --as-of 2025-03-02T00:00:00Z
          --output restatement-replay.json
      - run: cmp restatement.json restatement-replay.json
      - run: >-
          uv run python -m statement_lens.cli fixtures/synthetic_ratio_statement.json
          --ratio-policy fixtures/synthetic_ratio_policy.json
          --output ratios.json
      - run: >-
          uv run python -m statement_lens.cli fixtures/synthetic_ratio_statement.json
          --ratio-policy fixtures/synthetic_ratio_policy.json
          --output ratios-replay.json
      - run: cmp ratios.json ratios-replay.json
      - run: uv build
      - run: uv pip check
      - run: uv export --frozen --no-dev --no-emit-project --format requirements-txt --output-file runtime-requirements.txt
      - run: uv run pip-audit --requirement runtime-requirements.txt --strict
